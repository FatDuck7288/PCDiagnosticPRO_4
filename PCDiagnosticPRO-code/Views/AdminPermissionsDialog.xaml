<Window x:Class="PCDiagnosticPro.Views.AdminPermissionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Autorisations requises" 
        Height="380" Width="480"
        MinHeight="340" MinWidth="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF1A1A2E">

    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFE6EDF3"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FF8B949E"/>
        <SolidColorBrush x:Key="AccentRed" Color="#FFFF4757"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF2ECC71"/>
        <SolidColorBrush x:Key="BackgroundMedium" Color="#FF21222C"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF30363D"/>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🔒 Autorisations requises" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Pour un fonctionnement optimal, l'application nécessite les autorisations suivantes."
                       FontSize="12" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Permission checklist -->
        <StackPanel Grid.Row="1">
            <!-- UAC Elevation -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🛡️" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Droits administrateur" FontWeight="SemiBold" FontSize="14"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Élévation UAC pour accéder aux informations système complètes"
                                   FontSize="11" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="AdminStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Windows Defender -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🦠" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Exclusions Windows Defender" FontWeight="SemiBold" FontSize="14"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Règles d'exclusion pour éviter les faux positifs pendant le scan"
                                   FontSize="11" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="DefenderStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Network Access -->
            <Border Background="{StaticResource BackgroundMedium}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🌐" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Accès réseau" FontWeight="SemiBold" FontSize="14"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Activation des tests de vitesse réseau et diagnostics connectivité"
                                   FontSize="11" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="NetworkStatusIcon" Grid.Column="2" Text="⏳" FontSize="18" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Action buttons -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText" Grid.Column="0" Text="" FontSize="12"
                       Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>

            <Button x:Name="CancelButton" Grid.Column="1" Content="Annuler" Width="100" Height="36"
                    Background="Transparent" Foreground="{StaticResource TextPrimary}"
                    BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                    Cursor="Hand" Margin="0,0,10,0"
                    Click="CancelButton_Click"/>

            <Button x:Name="AuthorizeButton" Grid.Column="2" Content="Autoriser" Width="120" Height="36"
                    Background="{StaticResource AccentRed}" Foreground="White"
                    BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"
                    Click="AuthorizeButton_Click"/>
        </Grid>
    </Grid>
</Window>
