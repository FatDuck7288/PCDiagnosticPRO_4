<Window x:Class="PCDiagnosticPro.Views.AdminPermissionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Autorisations requises"
        SizeToContent="WidthAndHeight"
        MinWidth="540"
        MaxWidth="760"
        MinHeight="380"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="#FF1A1A2E">

    <Window.Resources>
        <SolidColorBrush x:Key="DialogTextPrimaryBrush" Color="#FFE6EDF3"/>
        <SolidColorBrush x:Key="DialogTextSecondaryBrush" Color="#FF8B949E"/>
        <SolidColorBrush x:Key="DialogAccentGreenBrush" Color="#FF2ECC71"/>
        <SolidColorBrush x:Key="DialogBackgroundMediumBrush" Color="#FF21222C"/>
        <SolidColorBrush x:Key="DialogBorderBrush" Color="#FF30363D"/>
    </Window.Resources>

    <Grid Margin="24" MaxWidth="700">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Autorisations requises"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource DialogTextPrimaryBrush}"/>
            <TextBlock Text="Pour un fonctionnement optimal, l'application peut demander des permissions supplementaires."
                       FontSize="12"
                       Foreground="{StaticResource DialogTextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Border Background="{StaticResource DialogBackgroundMediumBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="{StaticResource DialogBorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Droits administrateur" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                        <TextBlock Text="Elevation UAC pour acceder aux informations systeme completes."
                                   FontSize="11" Foreground="{StaticResource DialogTextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="AdminStatusIcon" Grid.Column="2" Text="..." FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource DialogTextSecondaryBrush}"/>
                </Grid>
            </Border>

            <Border Background="{StaticResource DialogBackgroundMediumBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="{StaticResource DialogBorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="&#xE72A;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Exclusions Windows Defender" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                        <TextBlock Text="Regles d'exclusion pour eviter les faux positifs pendant le scan."
                                   FontSize="11" Foreground="{StaticResource DialogTextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="DefenderStatusIcon" Grid.Column="2" Text="..." FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource DialogTextSecondaryBrush}"/>
                </Grid>
            </Border>

            <Border Background="{StaticResource DialogBackgroundMediumBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,4" BorderBrush="{StaticResource DialogBorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Acces reseau" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource DialogTextPrimaryBrush}"/>
                        <TextBlock Text="Activation des tests de vitesse reseau et diagnostics de connectivite."
                                   FontSize="11" Foreground="{StaticResource DialogTextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="NetworkStatusIcon" Grid.Column="2" Text="..." FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource DialogTextSecondaryBrush}"/>
                </Grid>
            </Border>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       Text=""
                       FontSize="12"
                       Foreground="{StaticResource DialogTextSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,10,0"/>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    Content="Annuler"
                    MinWidth="110"
                    Height="36"
                    Background="Transparent"
                    Foreground="{StaticResource DialogTextPrimaryBrush}"
                    BorderBrush="{StaticResource DialogBorderBrush}"
                    BorderThickness="1"
                    Cursor="Hand"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>

            <Button x:Name="AuthorizeButton"
                    Grid.Column="2"
                    Content="Autoriser"
                    MinWidth="128"
                    Height="36"
                    Background="{DynamicResource AccentRedBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="AuthorizeButton_Click"/>
        </Grid>
    </Grid>
</Window>