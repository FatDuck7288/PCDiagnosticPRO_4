<Window x:Class="PCDiagnosticPro.Views.UpdateApplyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:PCDiagnosticPro.Controls"
        xmlns:conv="clr-namespace:PCDiagnosticPro.Converters"
        mc:Ignorable="d"
        Title="Application des mises a jour"
        Height="640"
        Width="1020"
        MinHeight="460"
        MinWidth="820"
        Background="{StaticResource BackgroundDarkBrush}"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource BackgroundMediumBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource AccentBlueBrush}" CornerRadius="4" Padding="10,5" Margin="0,0,8,0">
                        <TextBlock Text="{Binding TotalCount, StringFormat=Total: {0}}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="#FF58A6FF" CornerRadius="4" Padding="10,5" Margin="0,0,8,0">
                        <TextBlock Text="{Binding RunningCount, StringFormat=En cours: {0}}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="#FF2ED573" CornerRadius="4" Padding="10,5" Margin="0,0,8,0">
                        <TextBlock Text="{Binding SuccessCount, StringFormat=Reussies: {0}}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="{DynamicResource DangerBrush}" CornerRadius="4" Padding="10,5" Margin="0,0,8,0">
                        <TextBlock Text="{Binding FailedCount, StringFormat=Echouees: {0}}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="#FFFFA502" CornerRadius="4" Padding="10,5">
                        <TextBlock Text="{Binding SkippedCount, StringFormat=Ignorees: {0}}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Margin="18,0,18,0"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding HeaderStatus}"
                           TextTrimming="CharacterEllipsis"
                           ToolTip="{Binding HeaderStatus}"/>

                <ctrl:ThemedSpinner Grid.Column="2"
                                    Width="18"
                                    Height="18"
                                    VerticalAlignment="Center"
                                    IsActive="{Binding IsRunning}"
                                    Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  CanUserSortColumns="False"
                  Background="{StaticResource BackgroundMediumBrush}"
                  RowBackground="{StaticResource BackgroundMediumBrush}"
                  AlternatingRowBackground="{StaticResource BackgroundLightBrush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderBrush}">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="6,4"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding KindDisplay}" Width="95"/>
                <DataGridTemplateColumn Header="Statut" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StateDisplay}"
                                       Foreground="{Binding StateBrush}"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Progression" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <ProgressBar Height="14"
                                             Minimum="0"
                                             Maximum="100"
                                             Value="{Binding Percent}"
                                             Foreground="{StaticResource AccentRedBrush}"
                                             Background="{StaticResource BackgroundLightBrush}"/>
                                <TextBlock Text="{Binding Percent, StringFormat={}{0}%}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           FontSize="10"
                                           FontWeight="SemiBold"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Details" Binding="{Binding Message}" Width="280"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2"
                Background="{StaticResource BackgroundMediumBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10"
                Margin="0,12,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding GlobalPercent, StringFormat=Progression globale: {0:F0}%}"
                           Margin="0,0,0,6"/>
                <ProgressBar Grid.Row="1"
                             Height="16"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding GlobalPercent}"
                             Foreground="{StaticResource AccentRedBrush}"
                             Background="{StaticResource BackgroundLightBrush}"/>
            </Grid>
        </Border>

        <Expander Grid.Row="3"
                  Header="Log technique"
                  IsExpanded="{Binding IsLogExpanded}"
                  Margin="0,10,0,0"
                  Background="{StaticResource BackgroundMediumBrush}"
                  Foreground="{StaticResource TextPrimaryBrush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1">
            <Border Background="{StaticResource BackgroundLightBrush}" Padding="8">
                <ListBox ItemsSource="{Binding LogLines}"
                         Background="{StaticResource BackgroundLightBrush}"
                         BorderThickness="0"
                         Foreground="{StaticResource TextSecondaryBrush}"
                         FontFamily="Consolas"
                         FontSize="11"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            </Border>
        </Expander>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="Annuler"
                    Command="{Binding CancelCommand}"
                    IsEnabled="{Binding CanCancel}"
                    Margin="0,0,10,0"
                    MinWidth="130"
                    Padding="16,8"
                    Background="{StaticResource BackgroundLightBrush}"
                    Foreground="{StaticResource TextPrimaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    FontWeight="SemiBold"/>

            <Button Grid.Column="2"
                    Content="Fermer"
                    Command="{Binding CloseCommand}"
                    IsEnabled="{Binding CanClose}"
                    Style="{StaticResource FuturisticButton}"
                    MinWidth="130"
                    Padding="16,8"/>
        </Grid>
    </Grid>
</Window>
